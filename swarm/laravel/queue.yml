version: '3.8'

services:

  queue:
    depends_on:
      - app

    image: *BASE_IMAGE*/queue:${VERSION}

    secrets:
      - source: laravel_env
        target: /var/www/html/.env

secrets:
  laravel_env:
    external: true
